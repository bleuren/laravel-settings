<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | 設定模型
    |--------------------------------------------------------------------------
    |
    | 指定用於處理設定的 Eloquent 模型類別。
    | 可以使用套件預設的 Setting 模型，或指定自定義模型。
    | 自定義模型需要使用 HasSettings trait 並包含必要的欄位。
    |
    */
    'model' => env('SETTINGS_MODEL', \Bleuren\LaravelSetting\Setting::class),

    /*
    |--------------------------------------------------------------------------
    | 資料庫配置
    |--------------------------------------------------------------------------
    |
    | 設定資料庫連接和表名稱。
    |
    */
    'database_connection' => env('SETTINGS_DB_CONNECTION', null),
    'table' => env('SETTINGS_TABLE', 'settings'),

    /*
    |--------------------------------------------------------------------------
    | 緩存配置
    |--------------------------------------------------------------------------
    |
    | 設定緩存前綴和策略。
    |
    */
    'cache_prefix' => env('SETTINGS_CACHE_PREFIX', 'settings.'),

    /*
    |--------------------------------------------------------------------------
    | 預載入配置
    |--------------------------------------------------------------------------
    |
    | 控制是否在應用啟動時預載入常用設定到記憶體中。
    | 只在 HTTP 請求中執行，避免在 console 命令中影響性能。
    |
    */
    'eager_load' => env('SETTINGS_EAGER_LOAD', false),

    /*
    |--------------------------------------------------------------------------
    | 預載入的設定鍵
    |--------------------------------------------------------------------------
    |
    | 當啟用預載入時，這些設定將在應用啟動時被載入到記憶體中。
    | 只包含最常用的設定以避免佔用過多記憶體。
    |
    | 範例：
    | 'eager_load_keys' => [
    |     'site.name',
    |     'site.logo',
    |     'app.theme',
    |     'maintenance.mode',
    | ],
    |
    */
    'eager_load_keys' => [
        // 在此處列出需要預載入的設定鍵
    ],

    /*
    |--------------------------------------------------------------------------
    | 效能配置
    |--------------------------------------------------------------------------
    |
    | 控制套件的效能相關設定。
    |
    */

    // 批量操作的最大數量（防止記憶體溢出）
    'batch_size' => env('SETTINGS_BATCH_SIZE', 100),

    // 是否啟用查詢日誌（除錯用）
    'enable_query_log' => env('SETTINGS_ENABLE_QUERY_LOG', false),
];
